---
description: Dispatch a Flutter Engine chat flow
---

Invoke `$flutter-engine` and pass the user's complete message unchanged. Treat
`flutter run flow:*` as a chat command, never as a terminal command. Resolve the
flow from `.specify/extensions/flutter-engine/references/commands/commands.md`.
