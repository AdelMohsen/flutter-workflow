---
description: Run a Flutter Engine operation without a plan gate
---

Invoke `$flutter-engine` for `chat`, `setup`, `onboard`, `test`, `resume`, or
`check`.
Respect each command's mutation boundary and existing gate state.
