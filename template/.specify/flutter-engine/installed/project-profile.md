# Flutter Engine project profile

Status: `NOT_ONBOARDED`

Run `flutter run flow:onboard` in Codex to replace this placeholder with
discovered project facts. Do not record guesses.

Onboarding records entrypoints, dependency direction, feature/repository/network
contracts, backend error mapping, routing/deep links, storage/security, logging,
localization, design/theme/assets, adaptive/responsive behavior, reusable UI and
form fields, platforms, generation, tests, verified commands, known debt, and
missing inputs. Foundation verification performs the same finalization for a
new project.
